# Dawn

git remote set-url origin https://github.com/shopifyexpertpro/peeters-theme.git


npm install -g @shopify/cli@latest



```bash
shopify theme check
```
git add .
git commit -m "update theme new"
git pull origin main --rebase
git push origin main