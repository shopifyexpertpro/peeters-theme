 
 
{% if media %}
    {% capture media_style %}
       height:auto;
    {% endcapture %}

    {{ media
        | image_url: width: '1200px'
        | image_tag:
        class: 'media_style',
        loading: 'lazy'
    }}
{% else %}
    {{ 'blog-apparel-1' | placeholder_svg_tag: 'icon' }}
{% endif %}